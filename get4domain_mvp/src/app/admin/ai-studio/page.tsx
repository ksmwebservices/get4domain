// Admin Platform mount of the existing vendor AI Studio (Stage 4). Reused as-is
// via re-export so Marketing staff get the same content-generation UI under the
// admin route — no duplicated component. Visible to SUPER_ADMIN + MARKETING.
export { default } from '@/app/dashboard/ai-studio/page';
