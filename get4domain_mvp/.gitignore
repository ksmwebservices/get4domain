logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Environment - NEVER commit these
.env.local
.env.production

# Reference/upload material to port FROM — a standalone app with its own deps/tsconfig,
# never meant to build inside this repo (excluded from tsconfig.json too).
/reference/
tsconfig.tsbuildinfo
