name: Deploy Dev

on:
  push:
    branches: [develop]

jobs:
  deploy-dev:
    name: Deploy to development environment
    runs-on: ubuntu-latest
    environment: development
    steps:
      - uses: actions/checkout@v4

      # This job is a placeholder wired up during P006 (Deployment) for each
      # client project. It documents the expected shape of the dev deploy
      # pipeline for the platform repo itself and is not yet connected to a
      # live server.
      - name: Placeholder — no deploy target configured yet
        run: echo "Configure SSH/Docker deploy steps in P006."
